<div class="login">
  <div class="container">
    <div class="row m-auto">
      <div class="login-form">
        <div class="col-12 mt-3 text-center">
          <h1 class="title"><span><b>Login</b></span></h1>
          <h6 class="border-bottom border-secondary">ログイン</h6>
        </div>
        <div class="text-center", style="color: red">
          <% flash.each do |key, value| %>
            <%= content_tag(:div, value, class: "#{key}") %>
          <% end %>
        </div>
        <div class="input-form">
          <%= form_with model:@user, url: user_session_path, local: true do |f| %>
          <div class="form">
            <%= f.label :name, "ユーザー名" %>
            <%= f.text_field :name, autofocus: true, autocomplete: "name", class:"input" %>
          </div>
        
          <div class="form">
            <%= f.label :password, "パスワード" %>
            <%= f.password_field :password, autocomplete: "current-password", class:"input" %>
          </div>
        
          <div class="text-center my-3">
            <%= f.submit "ログイン", class:"btn btn-secondary btn-sm" %>
          </div>
          <% end %>
        </div>
      </div>
      <div class="login-form">
        <div class="col-12 mt-3 text-center">
          <span><b>会員登録がお済でない方</b></span>
          <div class="text-center mt-3">
            <%= link_to "新規会員登録", new_user_registration_path, class:"btn btn-secondary btn-sm" %>
          </div>
        </div>
      </div>
      <div class="login-form">
        <div class="col-12 mt-3 text-center">
          <span><b>ゲストログイン(閲覧用)</b></span></br>
          <span>ゲストユーザーの投稿は、日付が変わるごとに削除されます。</span>
          <div class="text-center mt-3">
            <%= link_to "ゲストログイン", users_guest_sign_in_path, method: :post, class:"btn btn-secondary btn-sm" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>