<div class="container my-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <%= render 'users/info', user: @user %>
    </div>
    <div class="col-md-8">
      <%= render 'schedules/form', schedule: @schedule, schedules: @schedules, note: @note, spot: @spot  %>
      <%= render 'spots/form', schedule: @schedule, schedules: @schedules, note: @note, spot: @spot  %>
      <%= link_to "投稿", notes_post_path(@note), method: :put %>
    </div>
  </div>
</div>