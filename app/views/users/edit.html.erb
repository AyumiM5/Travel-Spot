<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h2>User info</h2>
      <%= form_with model:@user,url: mypage_update_path, method: :patch, local: true do |f| %>
      <div class="form-group">
        <%= f.label :name %><br />
        <%= f.text_field :name, autofocus: true, autocomplete: "name" %>
      </div>
      <div class="form-group">
        <%= f.label :Image %><br />
        <%= f.attachment_field :profile_image %>
      </div>
      <div class="actions col-8 mx-auto">
        <%= f.submit "変更を確定する", class: "btn-block btn-sm btn btn-secondary" %>
      </div>
      <div class="actions col-8 mx-auto my-3">
        <%= link_to "退会する", withdraw_path, class: "btn-block btn-sm btn btn-secondary", method: :patch, "data-confirm" => "本当に退会しますか？" %>
      </div>
      <% end %>
    </div>
  </div>
</div>